# Vibe Coding Starter Kit

AIと一緒に開発する時に「毎回同じ説明するのめんどくさい」を解決するキット。

---

## こんな悩みありませんか？

- 毎回ファイル構成やルールをイチから説明してる
- 開発の途中で進捗がわからなくなる
- エージェントが終わったか分からなくて画面を見張ってる

**全部このキットで解決します。**

---

## キットの構成

このキットは **エージェントごとにフォルダが分かれています**。
自分が使うエージェントのフォルダだけ取り出して使います。

```
starter-kit/
├── shared/       ← 全エージェント共通（必ず使う）
├── claude/       ← Claude Code を使うならこれ
├── codex/        ← Codex を使うならこれ
├── gemini/       ← Gemini CLI を使うならこれ
├── cursor/       ← Cursor を使うならこれ
├── windsurf/     ← Windsurf を使うならこれ
└── setup.sh      ← 自動で取り出してくれるスクリプト
```

**shared/ は必ず必要**。それに加えて、自分が使うエージェントのフォルダを取り出す。
複数のエージェントを併用する場合は、複数フォルダを取り出す。

---

## 始め方

### 方法 1: エージェントに頼む（初心者向け）

エージェント (Claude Code / Cursor / Gemini 等) を開いて、こう言うだけ:

> ~/Desktop/starter-kit の中にバイブコーディングのスターターキットがあります。
> これを使って「予約管理アプリ」のプロジェクトを作ってください。
> 私は Claude Code を使っています。
> setup.sh を実行してから /init-project で初期化してください。

エージェントが自動で `shared/` + 指定したエージェントのフォルダを取り出して、
質問に答えるだけでプロジェクトが完成します。

### 方法 2: 自分でコマンドを打つ（慣れてる人向け）

```bash
# 「my-app」がプロジェクト名、「claude」が使うエージェント
bash ~/Desktop/starter-kit/setup.sh my-app claude

cd my-app && claude
> /init-project
```

他のエージェントの場合:
```bash
bash ~/Desktop/starter-kit/setup.sh my-app codex      # Codex
bash ~/Desktop/starter-kit/setup.sh my-app gemini      # Gemini
bash ~/Desktop/starter-kit/setup.sh my-app cursor      # Cursor
bash ~/Desktop/starter-kit/setup.sh my-app windsurf    # Windsurf
```

複数のエージェントを併用する場合:
```bash
bash ~/Desktop/starter-kit/setup.sh my-app claude cursor
```

### 初めて使う時のコツ

最初からエージェントに全部任せなくていい。段階的に信頼を築くのがおすすめ:

1. **まず質問してみる** — 「このプロジェクトの構成を説明して」と聞いて、答えの精度を確認
2. **小さいタスクを頼む** — 「この関数にテストを追加して」等、影響範囲が小さいもの
3. **徐々に任せる範囲を広げる** — 精度に満足したら、機能追加やリファクタを任せる

---

## 初期化の流れ

`/init-project` を実行すると、2つ聞かれます:

```
1. 要件定義のファイルはありますか？ → ある / ない
2. 壁打ち（対話で深掘り）しますか？  → する / しない
```

| パターン | 流れ |
|---------|------|
| ファイルあり + 壁打ちなし | ファイルを読んで自動設定 → すぐ生成 |
| ファイルあり + 壁打ちあり | ファイルをベースに「ここは本当にこれでいい？」を対話で深掘り → 生成 |
| ファイルなし + 壁打ちなし | 5つの質問に答える → 生成 |
| ファイルなし + 壁打ちあり | ゼロから「誰が困ってる？」「どう解決する？」を対話で整理 → 生成 |

壁打ちした場合は要件定義書 (docs/requirements.md) も自動で保存されます。

ファイルがない場合は5つ聞かれます:

```
1. プロジェクト名は？        → 予約管理アプリ
2. 何を作る？（一言で）       → 美容院の予約をネットで受け付ける
3. 技術スタック？             → Next.js + Supabase
4. どんな画面がある？(3〜5個) → 予約カレンダー、予約フォーム、管理画面
5. デプロイ先は？             → Vercel
```

どちらの場合でも、これが全部自動で作られます:
- 設計書 (docs/architecture.md)
- タスク一覧 (docs/tasks.md) ← チェックボックス付き
- GitHubリポジトリ
- ルール・スキル42個

**あとは「始めて」と言うだけ。**

---

## 開発中にできること

エージェントにこう言うだけで使えます。コマンドを覚える必要はありません。

| こう言えば | やってくれること |
|-----------|---------------|
| 「進捗見せて」 | いま何%終わったか進捗バーで見せてくれる |
| 「バックアップして」 | GitHubに保存してくれる |
| 「コミットして」 | 変更内容を見てコミットメッセージを作ってくれる |
| 「コードレビューして」 | 問題点をチェックして重要度別に教えてくれる |
| 「検索機能を計画して」 | タスクに分けて計画を立ててくれる（コードは書かない） |
| 「このバグ直して: ○○」 | 原因を見つけて直し方を出してくれる |
| 「セキュリティチェックして」 | 脆弱性がないか調べてくれる |
| 「テスト作って」 | 既存コードに合わせてテストを自動生成 |
| 「Dockerファイル作って」 | プロジェクトに合ったDockerfileを作ってくれる |
| 「CI/CD設定して」 | GitHub Actionsの設定を自動生成 |

> `/` を押すと全42コマンドが日本語の説明付きで出ます。

---

## チーム開発

HP制作やシステム開発で、フロント・バック・デザインを分けて進めたい時:

> 「チーム開発モードで始めて」

エージェントが「何を作る？」と聞いてきて、役割を振り分けて順番に進めてくれます。

---

## 引き継ぎ

途中で別の人やエージェントに渡したい時:

> 「引き継ぎ資料を作って」

HANDOFF.md が自動で作られます。引き継いだ人は:

> 「このプロジェクトを引き継いだ。HANDOFF.md を読んで再開して」

---

## 対応エージェント

| エージェント | setup.sh での名前 |
|------------|-----------------|
| Claude Code | `claude` |
| Codex (OpenAI) | `codex` |
| Gemini CLI | `gemini` |
| Cursor | `cursor` |
| Windsurf | `windsurf` |

複数使う場合は並べるだけ: `bash setup.sh my-app claude cursor`

---

## 全42スキル

### 管理

| コマンド | やること |
|---------|---------|
| `/init-project` | 質問に答えるだけでプロジェクトを丸ごと作る |
| `/progress` | いま何%終わったか進捗バーで見る |
| `/backup` | GitHubにバックアップする |
| `/save-skill` | 便利だったやり方をスキルとして保存する |
| `/sync-agents` | ルールを全エージェントに反映する |
| `/handoff` | 別の人やエージェントに引き継ぐための資料を作る |
| `/resume` | 引き継ぎ資料を読んで開発を再開する |
| `/taskboard` | チェックボックス付きタスク一覧を作ってリアルタイム更新する |
| `/team-start` | チーム開発モードで始める。役割を振り分けて進める |
| `/team-status` | 各担当の進捗をまとめて報告する |
| `/autopilot` | 計画の承認後、ゴールまで全自動で走る（起動時に確認あり） |

### 品質

| コマンド | やること |
|---------|---------|
| `/review` | コードの問題点をチェックして重要度別に報告する |
| `/security-check` | セキュリティの穴がないか調べる |
| `/test-gen` | 既存コードに合わせてテストを自動で作る |
| `/commit` | 変更内容からコミットメッセージを作って確認後コミットする |
| `/polish` | さっき書いたコードをもう一回見直してシンプルにする |
| `/dead-code` | 使ってないコードを見つけて消す |
| `/land` | テスト→リント→コミット→push→次タスク提案まで全自動で締めくくる |

### 設計・開発

| コマンド | やること |
|---------|---------|
| `/plan` | 機能をタスクに分けて計画する。コードは書かない |
| `/spec` | 仕様書を作って認識のズレを事前に潰す |
| `/diagnose` | バグの原因を見つけて直し方を出す |
| `/refactor` | コードを整理してきれいにする |
| `/pr` | プルリクエストを作る |
| `/brainstorm` | コードを書く前にアイデアを深掘りする |
| `/architecture` | 設計のお作法(SOLID等)をチェックする |

### テスト

| コマンド | やること |
|---------|---------|
| `/tdd` | テストを先に書いてからコードを書く流れで進める |
| `/e2e` | ブラウザを自動で動かしてテストする |
| `/seed` | 本物っぽいテスト用ダミーデータを作る |

### セキュリティ

| コマンド | やること |
|---------|---------|
| `/secret-scan` | コードやgit履歴からAPIキー漏れを検出する |

### インフラ・デプロイ

| コマンド | やること |
|---------|---------|
| `/docker` | Dockerfile/Composeを作る・最適化する |
| `/ci-cd` | pushしたら自動テスト＆デプロイの設定を作る |
| `/db-migrate` | データベースの構造変更ファイルを作る |

### API

| コマンド | やること |
|---------|---------|
| `/api-design` | APIの設計ルール(URL命名・エラー形式等)をチェックする |

### ドキュメント

| コマンド | やること |
|---------|---------|
| `/docs` | コードからドキュメントを自動で作る |
| `/explain` | コードの仕組みを図付きでわかりやすく説明する |
| `/changelog` | 「何が変わったか」のリストをgitの記録から自動で作る |
| `/onboard` | 新しく来た人向けのプロジェクト入門ガイドを作る |

### 分析

| コマンド | やること |
|---------|---------|
| `/perf` | 遅い部分を見つけて速くする方法を出す |
| `/deps` | 使ってるパッケージの問題(脆弱性・古さ等)をチェックする |
| `/a11y` | 目が見えにくい人でも使えるかチェックする |

### AI・文章

| コマンド | やること |
|---------|---------|
| `/humanize` | AI臭い文章を自然な日本語に直す |
| `/self-learn` | 作業パターンを見つけてスキルにする |

