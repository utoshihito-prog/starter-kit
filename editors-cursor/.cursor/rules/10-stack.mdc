---
description: スタック固有の規約
globs: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.py", "**/*.go", "**/*.rb", "**/*.rs"]
---
# スタック規約

- 使用言語の strict mode を有効にする
- 型安全: 曖昧な型は避ける。具体的な型を使う
- ファイル名: 言語の慣例に従う
- インポート順序: 外部 → 内部 → 型 → スタイル
- 未使用のインポート・変数は即削除
- 関数は短く。1つの関数は1つの仕事だけ
- 既存のパターンに従う。新パターンを導入する前に既存を確認
